import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'smart-pregnancy-assistant',
  webDir: 'dist'
};

export default config;
